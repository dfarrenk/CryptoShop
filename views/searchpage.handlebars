<!-- // should setup a if statement -->
{{#if user}} 
{{> navbar_user}} 
{{else}} 
{{> navbar_visitor}} 
{{/if}}
<!-- All of this Content Below to the script tags, need to be split into Handlebar format-->
<!-- Page Wrapper-->
<div id="wrapper">
  {{> sidebar}}
  <!-- Page Content -->
  <div id="page-content-wrapper">
    <div class="container-fluid">
      <div class="col">
        <ul class="nav nav-tabs justify-content-center">
          <li class="nav-item">
            <a class="btn nav-link" data-target=".showEbay" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="showEbay">eBay</a>
          </li>
          <!--<li class="nav-item">-->
            <!--  <a class="btn nav-link" data-target=".showAmazon" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="showAmazon">Amazon</a>-->
            <!--</li>-->
            <!--<li class="nav-item">-->
              <!--  <a class="btn nav-link" data-target=".showWalmart" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="showWalmart">Wal-mart</a>-->
              <!--</li>-->
            </ul>
          </div>
          <!-- Card model will get looped over to dynamically create in handlebars-->
          <div class="loader"></div>
          <div class="row text-center" id="productMaster">
            <div class="jumbotron jumbotron-fluid" id="hideMeOnSearch">
              <div class="container">
                <h1 class="display-4">Search Results Will be Displayed here</h1>
                <p class="lead">Start by using the search box above!</p>
              </div>
            </div>
            <div class="row text-center" id="productDisplay"></div>
            <!-- Ebay search results -->
            <!-- Walmart Search Results -->
          </div>
        </div>
      </div>
      <!-- /#page-content-wrapper -->
    </div>
    <!-- /#wrapper -->
    <!-- /#wrapper -->
    {{> footer}} 
    {{> modal_login}} 
    {{> modal_error}}
    <div class="modal" tabindex="-1" role="dialog" id="buyItNowModal">
      <div class="modal-dialog" role="document">
        <div class="modal-content">
          <div class="modal-header">
            <h5 class="modal-title">You want to order</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
              <span aria-hidden="true">&times;</span>
            </button>
          </div>
          <div class="modal-body">
            <h3 id="modalName">Title</h3>
            <img src="" alt="of item" id="modalImg">
            <p id="modalPrice">Price: </p>
            <button id="paymentBtn">Payment</button>
            <div id="paymentInfo"></div>
          </div>
          <div class="modal-footer">
            <div id="countdownExample">
              <div class="values"></div>
            </div>
            <button type="button" class="btn btn-primary" id="placeOrderBtn">Make purchase</button>
            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          </div>
        </div>
      </div>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.0.4/socket.io.js"></script>

      {{> scripts}}
    </div>
    <script type="text/javascript">
      ReactDOM.render(
        React.createElement(UserAuth, { switch: "user_auth" }, null),
        document.getElementById("root")
        );
      </script>

